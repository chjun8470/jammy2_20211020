<%
/************************************************************************************
Generated by AIDesigner 5.6.0.3
************************************************************************************
보고서 제목 : 통계
작성일 : 2016년4월22일 10시42분9초
수정일 : 2016년8월12일 18시16분15초
작성자 :
************************************************************************************/
%>

<%@ page pageEncoding="UTF-8" %>
<%@ page contentType="text/html; charset=utf-8" %>

<%@ page import="java.sql.*,java.io.*,java.lang.*,java.text.*,java.util.*,java.net.URL" %>
<%@ page import="com.activeintra.manager.*" %>
<%@ page import="com.activeintra.url.*" %>
<%@ page import="com.activeintra.util.uds.*" %>
<%@ page import="org.apache.log4j.Logger" %>
<%@ page import="org.apache.log4j.PropertyConfigurator" %>


<%
Object[] userObj = null;
HttpParameterDecoder decoder = HttpParameterDecoder.newInstance(request, application);
request.setAttribute("paramsDecoder", decoder);

String reportMode = decoder.getParameter("reportMode");
String reportParams = decoder.getParameter("reportParams");
if(reportMode!=null){
	if(reportMode.equals("PDF")){
		if(reportParams == null || reportParams.toLowerCase().indexOf("pdfserversave:true") == -1){
			out.clear();
			out=pageContext.pushBody();
		}
	}
	else if(reportMode.equals("EXCEL")){
		if(reportParams == null || reportParams.toLowerCase().indexOf("excelserversave:true") == -1){
			out.clear();
			out=pageContext.pushBody();
		}
	}
	else if(reportMode.equals("AR5")){
		out.clear();
		out=pageContext.pushBody();
	}
}

ReportHandler handler = new ReportHandler(pageContext, request, response, userObj, decoder);
handler.service(out);
handler = null;
%>

<%!
private ReportInitializer initializer;
//보고서 초기화 - 한번만 실행되고 사용자 Request에서 반복사용가능한 코드를 기술
public void jspInit() {
	initializer = new ReportInitializer(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
	//수정되지 않는 정적 Query변수들을 초기화
	//sql_getJnspReseMon
	StringBuilder sql_getJnspReseMon = new StringBuilder();
	sql_getJnspReseMon.setLength(0);
	sql_getJnspReseMon.append("SELECT ");
	sql_getJnspReseMon.append("				DATE_FORMAT(ORDER_DT,'%Y년%m월') AS AXIS, ");
	sql_getJnspReseMon.append("				ORDER_STATE, ");
	sql_getJnspReseMon.append("				ORDER_DT, ");
	sql_getJnspReseMon.append("				COUNT(*) AS CNT ");
	sql_getJnspReseMon.append("			FROM ");
	sql_getJnspReseMon.append("				TB_EQU_ORDER ");
	sql_getJnspReseMon.append("			WHERE 1 = 1 ");
	sql_getJnspReseMon.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseMon.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -13 MONTH) ");
	sql_getJnspReseMon.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseMon.append("ORDER BY AXIS ASC ");
	initializer.prepareQuery("sql_getJnspReseMon", sql_getJnspReseMon.toString());

	//sql_getJnspReseMonTt
	StringBuilder sql_getJnspReseMonTt = new StringBuilder();
	sql_getJnspReseMonTt.setLength(0);
	sql_getJnspReseMonTt.append("SELECT ");
	sql_getJnspReseMonTt.append("				DATE_FORMAT(ORDER_DT,'%Y년%m월') AS AXIS, ");
	sql_getJnspReseMonTt.append("				ORDER_STATE, ");
	sql_getJnspReseMonTt.append("				ORDER_DT, ");
	sql_getJnspReseMonTt.append("				COUNT(*) AS CNT ");
	sql_getJnspReseMonTt.append("			FROM ");
	sql_getJnspReseMonTt.append("				TB_EQU_ORDER ");
	sql_getJnspReseMonTt.append("			WHERE 1 = 1 ");
	sql_getJnspReseMonTt.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseMonTt.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -13 MONTH) ");
	sql_getJnspReseMonTt.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseMonTt.append("ORDER BY AXIS ASC ");
	initializer.prepareQuery("sql_getJnspReseMonTt", sql_getJnspReseMonTt.toString());

	//sql_getJnspReseMonTb
	StringBuilder sql_getJnspReseMonTb = new StringBuilder();
	sql_getJnspReseMonTb.setLength(0);
	sql_getJnspReseMonTb.append("SELECT ");
	sql_getJnspReseMonTb.append("				DATE_FORMAT(ORDER_DT,'%Y년%m월') AS AXIS, ");
	sql_getJnspReseMonTb.append("				ORDER_STATE, ");
	sql_getJnspReseMonTb.append("				ORDER_DT, ");
	sql_getJnspReseMonTb.append("				COUNT(*) AS CNT ");
	sql_getJnspReseMonTb.append("			FROM ");
	sql_getJnspReseMonTb.append("				TB_EQU_ORDER ");
	sql_getJnspReseMonTb.append("			WHERE 1 = 1 ");
	sql_getJnspReseMonTb.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseMonTb.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -13 MONTH) ");
	sql_getJnspReseMonTb.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseMonTb.append("ORDER BY AXIS DESC ");
	initializer.prepareQuery("sql_getJnspReseMonTb", sql_getJnspReseMonTb.toString());

	//sql_getJnspReseDay
	StringBuilder sql_getJnspReseDay = new StringBuilder();
	sql_getJnspReseDay.setLength(0);
	sql_getJnspReseDay.append("SELECT ");
	sql_getJnspReseDay.append("DATE_FORMAT(ORDER_DT,'%m월%d일') AS AXIS, ");
	sql_getJnspReseDay.append("ORDER_STATE, ");
	sql_getJnspReseDay.append("				ORDER_DT, ");
	sql_getJnspReseDay.append("				COUNT(*) AS CNT ");
	sql_getJnspReseDay.append("			FROM ");
	sql_getJnspReseDay.append("				TB_EQU_ORDER ");
	sql_getJnspReseDay.append("			WHERE 1 = 1 ");
	sql_getJnspReseDay.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseDay.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -11 DAY) ");
	sql_getJnspReseDay.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseDay.append("GROUP BY AXIS ");
	sql_getJnspReseDay.append("ORDER BY AXIS ASC ");
	initializer.prepareQuery("sql_getJnspReseDay", sql_getJnspReseDay.toString());

	//sql_getJnspReseDayTt
	StringBuilder sql_getJnspReseDayTt = new StringBuilder();
	sql_getJnspReseDayTt.setLength(0);
	sql_getJnspReseDayTt.append("SELECT ");
	sql_getJnspReseDayTt.append("DATE_FORMAT(ORDER_DT,'%m월%d일') AS AXIS, ");
	sql_getJnspReseDayTt.append("ORDER_STATE, ");
	sql_getJnspReseDayTt.append("				ORDER_DT, ");
	sql_getJnspReseDayTt.append("				COUNT(*) AS CNT ");
	sql_getJnspReseDayTt.append("			FROM ");
	sql_getJnspReseDayTt.append("				TB_EQU_ORDER ");
	sql_getJnspReseDayTt.append("			WHERE 1 = 1 ");
	sql_getJnspReseDayTt.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseDayTt.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -11 DAY) ");
	sql_getJnspReseDayTt.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseDayTt.append("GROUP BY AXIS ");
	sql_getJnspReseDayTt.append("ORDER BY AXIS ASC ");
	initializer.prepareQuery("sql_getJnspReseDayTt", sql_getJnspReseDayTt.toString());

	//sql_getJnspReseDayTb
	StringBuilder sql_getJnspReseDayTb = new StringBuilder();
	sql_getJnspReseDayTb.setLength(0);
	sql_getJnspReseDayTb.append("SELECT ");
	sql_getJnspReseDayTb.append("DATE_FORMAT(ORDER_DT,'%Y년%m월%d일') AS AXIS, ");
	sql_getJnspReseDayTb.append("ORDER_STATE, ");
	sql_getJnspReseDayTb.append("				ORDER_DT, ");
	sql_getJnspReseDayTb.append("				COUNT(*) AS CNT ");
	sql_getJnspReseDayTb.append("			FROM ");
	sql_getJnspReseDayTb.append("				TB_EQU_ORDER ");
	sql_getJnspReseDayTb.append("			WHERE 1 = 1 ");
	sql_getJnspReseDayTb.append("				AND ORDER_STATE = '2' ");
	sql_getJnspReseDayTb.append("AND ORDER_DT >= DATE_ADD(NOW(), INTERVAL -30 DAY) ");
	sql_getJnspReseDayTb.append("AND ORDER_DT <= CONCAT(DATE_FORMAT(ORDER_DT, NOW())) ");
	sql_getJnspReseDayTb.append("GROUP BY AXIS ");
	sql_getJnspReseDayTb.append("ORDER BY AXIS DESC ");
	initializer.prepareQuery("sql_getJnspReseDayTb", sql_getJnspReseDayTb.toString());
}

private Logger logger = Logger.getLogger("com.activeintra");
class ReportHandler{
	//공통 변수 선언
	private Object[] userObj = null;
	private int nStartPage;		//각 쉬트별 시작 페이지 저장
	private int nMaxPage;		//한 쉬트가 실행된 후 생성된 최대 페이지 번호 저장
	private int nPageNum;		//현재 페이지 번호
	private int nYStartPos;		//쉬트에서 Y 시작 좌표
	private int nYEndPos;		//쉬트에서 Y 끝 좌표
	private int nYCurPos;		//현재 Y 좌표
	private int nYSavePos;		//연결된 멀티 프레임 쉬트에서 시작 Y 좌표 저장
	private int nYLastPos;		//연결된 멀티 프레임 쉬트에서 각 프레임의 마지막 Y 좌표 저장
	private int nXStartPos;		//쉬트에서 X 시작 좌표
	private int nXEndPos;		//쉬트에서 X 끝 좌표
	private int nXCurPos;		//현재 X 좌표
	private int nXSavePos;		//연결된 멀티 프레임 쉬트에서 시작 X 좌표 저장
	private int  nXLastPos;		//연결된 멀티 프레임 쉬트에서 각 프레임의 마지막 X 좌표 저장
	private boolean bIsBeforePageSkip;		//조건 밴드에서 페이지 넘기기 여부 판단
	private int nLoopRef;		//절대 양식에서 루프 참조 변수
	private int CELL_OBJECT = 0;
	private int DRAW_OBJECT = 1;
	private String isEmpty="";		//빈 문자열 비교에 사용
	private Map<String, String> mapParam;

	private int nBandRepeatType;	//1이면 반복밴드 가로 반복
	private int nBandWidth;		//반복밴드의 가로 폭
	private int nBandOffset;		//반복밴드 가로 반복 offset값
	private int nYBandStart;		//반복밴드 가로 반복 Y 시작 좌표
	private int nYBandEnd;		//반복밴드 가로 반복 Y 끝 좌표
	private int nHorRepeatNum;		//반복밴드 가로 반복 횟수
	private int nHorRepeatCnt;		//반복밴드 가로 반복 횟수 카운팅
	private Map<String, Integer> mapLeft;		//셀 Left
	private Map<String, Integer> mapTop;		//셀 Top
	private Map<String, Integer> mapWidth;		//셀 Width
	private Map<String, Integer> mapHeight;		//셀 Height
	private Map<String, String> mapStyle;		//셀 Style
	private HttpServletRequest request;
	private HttpServletResponse response;
	private PageContext pageContext;
	private JspWriter out;
	private HttpParameterDecoder decoder;

	private AIScriptManager manager;
	private String reportMode="AIScript";
	private String pdfFilename="";
	private String excelFilename="";

	//-----데이터 변수 선언-----
	long sheet10_A1 = 0L;
	String sheet10_A2 = "";
	String sheet10_B20 = "";
	String sheet10_N20 = "";
	String AI_CHART_OBJ32;
	String sheet10_B21 = "";
	long sheet10_N21 = 0L;
	String sheet10_B22 = "";

	//-----Connection, Statement, ResultSet 객체 선언-----
	private Connection conn_jnsp; 	 //jnsp Connection 객체
	private java.sql.PreparedStatement stat_getJnspReseMon;		//ResultSet getJnspReseMon 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseMon;		//ResultSet getJnspReseMon 객체
	private java.sql.PreparedStatement stat_getJnspReseMonTt;		//ResultSet getJnspReseMonTt 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseMonTt;		//ResultSet getJnspReseMonTt 객체
	private java.sql.PreparedStatement stat_getJnspReseMonTb;		//ResultSet getJnspReseMonTb 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseMonTb;		//ResultSet getJnspReseMonTb 객체
	private java.sql.PreparedStatement stat_getJnspReseDay;		//ResultSet getJnspReseDay 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseDay;		//ResultSet getJnspReseDay 객체
	private java.sql.PreparedStatement stat_getJnspReseDayTt;		//ResultSet getJnspReseDayTt 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseDayTt;		//ResultSet getJnspReseDayTt 객체
	private java.sql.PreparedStatement stat_getJnspReseDayTb;		//ResultSet getJnspReseDayTb 객체를 생성하기 위한 PreparedStatement 객체
	private ResultSet getJnspReseDayTb;		//ResultSet getJnspReseDayTb 객체

	//-----SQL 변수 선언-----
	private StringBuilder sql_getJnspReseMon = new StringBuilder();		//ResultSet getJnspReseMon 객체를 생성하기 위한 쿼리문 저장
	private StringBuilder sql_getJnspReseMonTt = new StringBuilder();		//ResultSet getJnspReseMonTt 객체를 생성하기 위한 쿼리문 저장
	private StringBuilder sql_getJnspReseMonTb = new StringBuilder();		//ResultSet getJnspReseMonTb 객체를 생성하기 위한 쿼리문 저장
	private StringBuilder sql_getJnspReseDay = new StringBuilder();		//ResultSet getJnspReseDay 객체를 생성하기 위한 쿼리문 저장
	private StringBuilder sql_getJnspReseDayTt = new StringBuilder();		//ResultSet getJnspReseDayTt 객체를 생성하기 위한 쿼리문 저장
	private StringBuilder sql_getJnspReseDayTb = new StringBuilder();		//ResultSet getJnspReseDayTb 객체를 생성하기 위한 쿼리문 저장

	//-----쿼리 필드 변수 선언-----
	//ResultSet getJnspReseMon 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseMon_AXIS;
	private String getJnspReseMon_ORDER_STATE;
	private String getJnspReseMon_ORDER_DT;
	private long getJnspReseMon_CNT;

	//ResultSet getJnspReseMonTt 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseMonTt_AXIS;
	private String getJnspReseMonTt_ORDER_STATE;
	private String getJnspReseMonTt_ORDER_DT;
	private long getJnspReseMonTt_CNT;

	//ResultSet getJnspReseMonTb 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseMonTb_AXIS;
	private String getJnspReseMonTb_ORDER_STATE;
	private String getJnspReseMonTb_ORDER_DT;
	private long getJnspReseMonTb_CNT;

	//ResultSet getJnspReseDay 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseDay_AXIS;
	private String getJnspReseDay_ORDER_STATE;
	private String getJnspReseDay_ORDER_DT;
	private long getJnspReseDay_CNT;

	//ResultSet getJnspReseDayTt 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseDayTt_AXIS;
	private String getJnspReseDayTt_ORDER_STATE;
	private String getJnspReseDayTt_ORDER_DT;
	private long getJnspReseDayTt_CNT;

	//ResultSet getJnspReseDayTb 객체에서 필드 값을 받는 쿼리 필드 변수
	private String getJnspReseDayTb_AXIS;
	private String getJnspReseDayTb_ORDER_STATE;
	private String getJnspReseDayTb_ORDER_DT;
	private long getJnspReseDayTb_CNT;

	//ReportHandler 생성자 함수
	ReportHandler(PageContext pageContext, HttpServletRequest request, HttpServletResponse response, Object[] userObj, HttpParameterDecoder decoder){
		this.pageContext = pageContext;
		this.request = request;
		this.response = response;
		this.userObj = userObj;
		this.decoder = decoder;
	}

	//ReportHandler 서비스 함수
	void service(JspWriter out) throws Exception{
		try{    //try 구문
			//변수 초기화
			this.out = out;
			nMaxPage = 0;   //nMaxPage 초기화

			nBandRepeatType=0;
			nBandWidth=0;
			nBandOffset=0;
			nYBandStart=0;
			nYBandEnd=0;
			nHorRepeatNum=0;
			nHorRepeatCnt=0;

			manager = new AIScriptManager(request, response, pageContext, out, logger, null);

			ServletContext context = pageContext.getServletContext();
			String temp = null;
			if(!context.getContextPath().equals("/"))
			temp = request.getRequestURI().replace(context.getContextPath(), "");
			else
			temp = request.getRequestURI();

			String path = temp.replace(".jsp", ".style");

			//셀 Rect, Style 값 저장
			setCellInfoMap();

			//파라미터 설정 함수 호출
			setParam();
			if(manager.open(reportMode, context.getRealPath(path)))
			{
				//스크립트 헤더 함수 호출
				getScriptHeader("A4", "2100x2970", "9", "VERTICAL", "56 70 56 70");

				//고정 데이터 초기화 함수 호출
				initConstVarient();

				//Connection 및 Statement 객체 생성 함수 호출
				createConnAndStateObject();

				//쿼리문 초기화 함수 호출
				initSQLConst();

				//쉬트 함수 호출
				//SHEET=Sheet1   FRAME=0 함수 호출
				executeSheet10();

				//---------------스크립트 종료---------------
				manager.scriptWrite("/AIReport", "", "", false);
				manager.close();
			}
		}catch(Exception e){    //catch 구문
			logger.error("---service function exception raised---");
			logger.error(e);
			throw e;
		}finally{    //finally 구문
			//DataSet Close
			cleanup(getJnspReseDayTb, stat_getJnspReseDayTb, null, null);
			cleanup(getJnspReseDayTt, stat_getJnspReseDayTt, null, null);
			cleanup(getJnspReseDay, stat_getJnspReseDay, null, null);
			cleanup(getJnspReseMonTb, stat_getJnspReseMonTb, null, null);
			cleanup(getJnspReseMonTt, stat_getJnspReseMonTt, null, null);
			cleanup(getJnspReseMon, stat_getJnspReseMon, null, null);
			cleanup(null, null, null, conn_jnsp);
		}    //end try
	}

	//-----쉬트 함수-----
	//-----SHEET=Sheet1   FRAME=Frame0 Start
	//-----폼 형식 : 상대 양식
	private void executeSheet10( ) throws Exception{
		//쉬트 초기화 함수 호출
		getSheetPaperInfo("User Define", "2700x2970", "256", "VERTICAL", "7 0 7 0");
		initSheetVariant(19, 12, 747, 831, true);

		//시작 페이지 번호 출력
		pageWrite(nPageNum);

		//밴드 함수 호출
		getScriptFromyyyyy(true, false);    //Void 밴드 호출
		getScriptFromtest(true, true);    //Void 밴드 호출
		getScriptFromTbList(true);    //반복 밴드 호출
		getScriptFromttt(true, true);    //Void 밴드 호출

		//페이지 및 좌표 설정
		nMaxPage=(int)MAX((long)nMaxPage, (long)nPageNum);    //생성된 최대 페이지 번호 구하기
	}

	//-----밴드 함수-----
	//----yyyyy VoidBand Start----
	//----소속 쉬트 : SHEET=Sheet1    FRAME=Frame0
	//----밴드 이름 : yyyyy
	//----밴드 종류 : Void 밴드
	//----bIsFrist : 부모 밴드 함수나 쉬트 함수에서 최초로 호출될 때 true, 그 이후 호출될 때 false
	//----bIsPrint : 감추기 속성 설정 여부. true면 출력, false면 AI 스크립트를 생성하지 않음.
	//----nRepeatCnt : 부모 반복 밴드 수행 횟수와 연동 옵션 설정시 void 밴드가 수행할 반복 횟수 전달
	private void getScriptFromyyyyy(boolean bIsFirst, boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=20;    //밴드 출력 높이

		if(bIsFirst){   //밴드 최초 호출 시에만 실행되는 코드
			//DataSet 객체 생성
			//ResultSet getJnspReseDayTt 객체의 생성
			if(getJnspReseDayTt==null){
				getJnspReseDayTt = stat_getJnspReseDayTt.executeQuery();
			}else{
				getJnspReseDayTt.beforeFirst();
			}

			//데이터 fetch
			if(getJnspReseDayTt.next()){
				setFieldVariableFromgetJnspReseDayTt();	//ResultSet getJnspReseDayTt에서 필드 값 설정
			}else{
				setFieldInitFromgetJnspReseDayTt();	//ResultSet getJnspReseDayTt의 필드 값 초기화
			}

			//데이터 변수 초기화
			sheet10_A1=0L;

			//데이터 변수 할당
			sheet10_A1 = getJnspReseDayTt_CNT;
		}    //end if(bIsFirst)

		//AI 스크립트 출력
		if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
			//스크립트 출력 영역 검사
			if(isPageSkip(nYCurPos+nBandHeight)){
				pageSkip(10);
			}

			//스크립트 출력 함수 호출
			writeScriptyyyyy();

			//다음 출력 좌표 설정
			nYCurPos=nYCurPos+nBandHeight;
		}   //end if(bIsPrint)
	}

	//----test VoidBand Start----
	//----소속 쉬트 : SHEET=Sheet1    FRAME=Frame0
	//----밴드 이름 : test
	//----밴드 종류 : Void 밴드
	//----bIsFrist : 부모 밴드 함수나 쉬트 함수에서 최초로 호출될 때 true, 그 이후 호출될 때 false
	//----bIsPrint : 감추기 속성 설정 여부. true면 출력, false면 AI 스크립트를 생성하지 않음.
	//----nRepeatCnt : 부모 반복 밴드 수행 횟수와 연동 옵션 설정시 void 밴드가 수행할 반복 횟수 전달
	private void getScriptFromtest(boolean bIsFirst, boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=392;    //밴드 출력 높이

		if(bIsFirst){   //밴드 최초 호출 시에만 실행되는 코드
			//데이터 변수 초기화
			AI_CHART_OBJ32="";

			//데이터 변수 할당
			sheet10_A2="    JEINET 활용통계";
			sheet10_B20="일별";
			sheet10_N20="예약수";
			getChartDataAI_CHART_OBJ32( );
		}    //end if(bIsFirst)

		if(getJnspReseDayTt_CNT != 0 )
		{
		}
		else
		{
			return;
		}
		//AI 스크립트 출력
		if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
			//스크립트 출력 영역 검사
			if(isPageSkip(nYCurPos+nBandHeight)){
				pageSkip(10);
			}

			//스크립트 출력 함수 호출
			writeScripttest();

			//다음 출력 좌표 설정
			nYCurPos=nYCurPos+nBandHeight;
		}   //end if(bIsPrint)
	}

	//-----TbList RepeatBand Start-----
	//-----소속 쉬트 : SHEET=Sheet1   FRAME=Frame0
	//-----밴드 이름 : TbList
	//-----밴드 종류 : 반복 밴드
	//-----bIsPrint : 감추기 속성 설정 여부 true면 출력, false면 스크립트를 생성하지 않음
	private void getScriptFromTbList(boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=20;    //밴드 출력 높이
		boolean bIsFirst=true;

		//데이터 변수 초기화
		sheet10_B21="";
		sheet10_N21=0L;

		//DataSet 객체 생성
		//ResultSet getJnspReseDayTb 객체의 생성
		if(getJnspReseDayTb==null){
			getJnspReseDayTb = stat_getJnspReseDayTb.executeQuery();
		}else{
			getJnspReseDayTb.beforeFirst();
		}

		if(getJnspReseDayTt_CNT != 0 )
		{
		}
		else
		{
			return;
		}
		//--while Loop Start--##
		while(getJnspReseDayTb.next()){   //자동으로 생성되는 반복 밴드의 루프문
			//데이터 fetch
			setFieldVariableFromgetJnspReseDayTb();	//RecordSet getJnspReseDayTb에서 필드 값 설정

			if(bIsFirst){   //밴드 호출 후 루프 내에서 한번 만 실행되는 코드
			}   //end if(bIsFirst)

			//데이터 변수 할당
			sheet10_B21 = getJnspReseDayTb_AXIS;
			sheet10_N21 = getJnspReseDayTb_CNT;

			//AI 스크립트 출력
			if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
				if(isPageSkip(nYCurPos+nBandHeight)){   //스크립트 출력 영역 검사
					//페이지 스킵 함수 호출
					pageSkip(10);
				}   //end if(page skip test)

				//스크립트 출력 함수 호출
				writeScriptTbList();

				//다음 출력 좌표 설정
				nYCurPos=nYCurPos+nBandHeight;
			}   //end if(bIsPrint)

			//bIsFirst 및 출력 라인 설정
			bIsFirst=false;
		}   //while end

		if(bIsFirst){   //while 루프가 실행되지 않은 경우
		} else {   //while 루프가 실행된 경우
		}   //루프 외부 end if(bIsFirst)
	}

	//----ttt VoidBand Start----
	//----소속 쉬트 : SHEET=Sheet1    FRAME=Frame0
	//----밴드 이름 : ttt
	//----밴드 종류 : Void 밴드
	//----bIsFrist : 부모 밴드 함수나 쉬트 함수에서 최초로 호출될 때 true, 그 이후 호출될 때 false
	//----bIsPrint : 감추기 속성 설정 여부. true면 출력, false면 AI 스크립트를 생성하지 않음.
	//----nRepeatCnt : 부모 반복 밴드 수행 횟수와 연동 옵션 설정시 void 밴드가 수행할 반복 횟수 전달
	private void getScriptFromttt(boolean bIsFirst, boolean bIsPrint) throws Exception{
		//지역 변수 선언 및 초기화
		int nBandHeight=20;    //밴드 출력 높이

		if(bIsFirst){   //밴드 최초 호출 시에만 실행되는 코드
			//데이터 변수 할당
			sheet10_B22="데이터가 없습니다";
		}    //end if(bIsFirst)

		if(getJnspReseDayTt_CNT == 0 )
		{
		}
		else
		{
			return;
		}
		//AI 스크립트 출력
		if(bIsPrint){   //감추기 속성이 지정되지 않으면 bIsPrint는 true
			//스크립트 출력 영역 검사
			if(isPageSkip(nYCurPos+nBandHeight)){
				pageSkip(10);
			}

			//스크립트 출력 함수 호출
			writeScriptttt();

			//다음 출력 좌표 설정
			nYCurPos=nYCurPos+nBandHeight;
		}   //end if(bIsPrint)
	}

	//-----스크립트 출력 함수-----
	//----yyyyy 스크립트 문 출력 함수----
	private void writeScriptyyyyy( ) throws Exception{
		//스크립트문 생성
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_A1"), nYCurPos+mapTop.get("sheet10_A1"), mapWidth.get("sheet10_A1"), mapHeight.get("sheet10_A1"), String.valueOf(sheet10_A1), "sheet10_A1", mapStyle.get("sheet10_A1"));
	}

	//----test 스크립트 문 출력 함수----
	private void writeScripttest( ) throws Exception{
		//스크립트문 생성
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_A2"), nYCurPos+mapTop.get("sheet10_A2"), mapWidth.get("sheet10_A2"), mapHeight.get("sheet10_A2"), sheet10_A2, "sheet10_A2", mapStyle.get("sheet10_A2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_J2"), nYCurPos+mapTop.get("sheet10_J2"), mapWidth.get("sheet10_J2"), mapHeight.get("sheet10_J2"), "", "sheet10_J2", mapStyle.get("sheet10_J2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_K2"), nYCurPos+mapTop.get("sheet10_K2"), mapWidth.get("sheet10_K2"), mapHeight.get("sheet10_K2"), "", "sheet10_K2", mapStyle.get("sheet10_K2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_L2"), nYCurPos+mapTop.get("sheet10_L2"), mapWidth.get("sheet10_L2"), mapHeight.get("sheet10_L2"), "", "sheet10_L2", mapStyle.get("sheet10_L2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_M2"), nYCurPos+mapTop.get("sheet10_M2"), mapWidth.get("sheet10_M2"), mapHeight.get("sheet10_M2"), "", "sheet10_M2", mapStyle.get("sheet10_M2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_N2"), nYCurPos+mapTop.get("sheet10_N2"), mapWidth.get("sheet10_N2"), mapHeight.get("sheet10_N2"), "", "sheet10_N2", mapStyle.get("sheet10_N2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_O2"), nYCurPos+mapTop.get("sheet10_O2"), mapWidth.get("sheet10_O2"), mapHeight.get("sheet10_O2"), "", "sheet10_O2", mapStyle.get("sheet10_O2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_P2"), nYCurPos+mapTop.get("sheet10_P2"), mapWidth.get("sheet10_P2"), mapHeight.get("sheet10_P2"), "", "sheet10_P2", mapStyle.get("sheet10_P2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_Q2"), nYCurPos+mapTop.get("sheet10_Q2"), mapWidth.get("sheet10_Q2"), mapHeight.get("sheet10_Q2"), "", "sheet10_Q2", mapStyle.get("sheet10_Q2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_R2"), nYCurPos+mapTop.get("sheet10_R2"), mapWidth.get("sheet10_R2"), mapHeight.get("sheet10_R2"), "", "sheet10_R2", mapStyle.get("sheet10_R2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_S2"), nYCurPos+mapTop.get("sheet10_S2"), mapWidth.get("sheet10_S2"), mapHeight.get("sheet10_S2"), "", "sheet10_S2", mapStyle.get("sheet10_S2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_T2"), nYCurPos+mapTop.get("sheet10_T2"), mapWidth.get("sheet10_T2"), mapHeight.get("sheet10_T2"), "", "sheet10_T2", mapStyle.get("sheet10_T2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_U2"), nYCurPos+mapTop.get("sheet10_U2"), mapWidth.get("sheet10_U2"), mapHeight.get("sheet10_U2"), "", "sheet10_U2", mapStyle.get("sheet10_U2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_V2"), nYCurPos+mapTop.get("sheet10_V2"), mapWidth.get("sheet10_V2"), mapHeight.get("sheet10_V2"), "", "sheet10_V2", mapStyle.get("sheet10_V2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_W2"), nYCurPos+mapTop.get("sheet10_W2"), mapWidth.get("sheet10_W2"), mapHeight.get("sheet10_W2"), "", "sheet10_W2", mapStyle.get("sheet10_W2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_X2"), nYCurPos+mapTop.get("sheet10_X2"), mapWidth.get("sheet10_X2"), mapHeight.get("sheet10_X2"), "", "sheet10_X2", mapStyle.get("sheet10_X2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_Y2"), nYCurPos+mapTop.get("sheet10_Y2"), mapWidth.get("sheet10_Y2"), mapHeight.get("sheet10_Y2"), "", "sheet10_Y2", mapStyle.get("sheet10_Y2"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_B20"), nYCurPos+mapTop.get("sheet10_B20"), mapWidth.get("sheet10_B20"), mapHeight.get("sheet10_B20"), sheet10_B20, "sheet10_B20", mapStyle.get("sheet10_B20"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_N20"), nYCurPos+mapTop.get("sheet10_N20"), mapWidth.get("sheet10_N20"), mapHeight.get("sheet10_N20"), sheet10_N20, "sheet10_N20", mapStyle.get("sheet10_N20"));
		objectWrite(DRAW_OBJECT, nXCurPos+1, nYCurPos+33, 726, 322, AI_CHART_OBJ32, "AI_CHART_OBJ32", "draw_style_0");
	}

	//----TbList 스크립트 문 출력 함수----
	private void writeScriptTbList(  ) throws Exception{
		//스크립트문 생성
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_B21"), nYCurPos+mapTop.get("sheet10_B21"), mapWidth.get("sheet10_B21"), mapHeight.get("sheet10_B21"), sheet10_B21, "sheet10_B21", mapStyle.get("sheet10_B21"));
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_N21"), nYCurPos+mapTop.get("sheet10_N21"), mapWidth.get("sheet10_N21"), mapHeight.get("sheet10_N21"), String.valueOf(sheet10_N21), "sheet10_N21", mapStyle.get("sheet10_N21"));
	}

	//----ttt 스크립트 문 출력 함수----
	private void writeScriptttt( ) throws Exception{
		//스크립트문 생성
		objectWrite(CELL_OBJECT, nXCurPos+mapLeft.get("sheet10_B22"), nYCurPos+mapTop.get("sheet10_B22"), mapWidth.get("sheet10_B22"), mapHeight.get("sheet10_B22"), sheet10_B22, "sheet10_B22", mapStyle.get("sheet10_B22"));
	}

	private void getBgImageScriptEx(int nIndex) throws Exception{
	}

	//-----Connection 및 Statement 객체 생성 함수-----
	private void createConnAndStateObject() throws Exception{
		//jnsp Connection 객체 생성
		Class.forName("com.mysql.jdbc.Driver");
		conn_jnsp = DriverManager.getConnection("jdbc:mysql://jnsp.re.kr:3306/jnspcms?useUnicode=true&characterEncoding=utf-8", "jnspcms", "jnspcms!@12");
	}

	//-----ResultSet, PreparedStatement, Connection, CallableStatement Close 함수-----
	private void cleanup(Object rs, PreparedStatement st, CallableStatement cs, Connection conn){
		if(rs != null){
			try{
				if(rs instanceof ResultSet){
					((ResultSet)rs).close();
				}else if(rs instanceof com.activeintra.util.uds.AIDataSource){
					((com.activeintra.util.uds.AIDataSource)rs).close();
				}else if(rs instanceof com.activeintra.util.uds.XMLSetHandler){
					((com.activeintra.util.uds.XMLSetHandler)rs).close();
				}else if(rs instanceof com.activeintra.util.uds.CSVSetHandler){
					((com.activeintra.util.uds.CSVSetHandler)rs).close();
				}else if(rs instanceof com.activeintra.util.uds.JSONSetHandler){
					((com.activeintra.util.uds.JSONSetHandler)rs).close();
				}
			}
			catch(SQLException e){
				logger.error(e);
			}
		}

		if(st != null){
			try{
				st.close();
			}
			catch(SQLException e){
				logger.error(e);
			}
		}

		if(cs != null){
			try{
				cs.close();
			}
			catch(SQLException e){
				logger.error(e);
			}
		}

		if(conn != null){
			try{
				conn.close();
			}
			catch(SQLException e){
				logger.error(e);
			}
		}
	}

	//-----SQL문 변수 초기화 함수-----
	private void initSQLConst() throws Exception {
		stat_getJnspReseMon = initializer.configPreparedStatement("sql_getJnspReseMon", mapParam, conn_jnsp, null);

		stat_getJnspReseMonTt = initializer.configPreparedStatement("sql_getJnspReseMonTt", mapParam, conn_jnsp, null);

		stat_getJnspReseMonTb = initializer.configPreparedStatement("sql_getJnspReseMonTb", mapParam, conn_jnsp, null);

		stat_getJnspReseDay = initializer.configPreparedStatement("sql_getJnspReseDay", mapParam, conn_jnsp, null);

		stat_getJnspReseDayTt = initializer.configPreparedStatement("sql_getJnspReseDayTt", mapParam, conn_jnsp, null);

		stat_getJnspReseDayTb = initializer.configPreparedStatement("sql_getJnspReseDayTb", mapParam, conn_jnsp, null);
	}

	//-----ResultSet 객체에서 필드 값 설정 함수-----
	//ResultSet getJnspReseMon 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseMon() throws Exception{
		getJnspReseMon_AXIS = GetB(getJnspReseMon.getString(1));
		getJnspReseMon_ORDER_STATE = GetB(getJnspReseMon.getString(2));
		getJnspReseMon_ORDER_DT = GetB(getJnspReseMon.getString(3));
		getJnspReseMon_CNT = getJnspReseMon.getLong(4);
	}

	//ResultSet getJnspReseMon 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseMon(){
		getJnspReseMon_AXIS = "";
		getJnspReseMon_ORDER_STATE = "";
		getJnspReseMon_ORDER_DT = "";
		getJnspReseMon_CNT = 0L;
	}

	//ResultSet getJnspReseMon 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseMon() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseMon.getObject(1));
		row.add(getJnspReseMon.getObject(2));
		row.add(getJnspReseMon.getObject(3));
		row.add(getJnspReseMon.getLong(4));

		return row;
	}

	//ResultSet getJnspReseMonTt 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseMonTt() throws Exception{
		getJnspReseMonTt_AXIS = GetB(getJnspReseMonTt.getString(1));
		getJnspReseMonTt_ORDER_STATE = GetB(getJnspReseMonTt.getString(2));
		getJnspReseMonTt_ORDER_DT = GetB(getJnspReseMonTt.getString(3));
		getJnspReseMonTt_CNT = getJnspReseMonTt.getLong(4);
	}

	//ResultSet getJnspReseMonTt 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseMonTt(){
		getJnspReseMonTt_AXIS = "";
		getJnspReseMonTt_ORDER_STATE = "";
		getJnspReseMonTt_ORDER_DT = "";
		getJnspReseMonTt_CNT = 0L;
	}

	//ResultSet getJnspReseMonTt 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseMonTt() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseMonTt.getObject(1));
		row.add(getJnspReseMonTt.getObject(2));
		row.add(getJnspReseMonTt.getObject(3));
		row.add(getJnspReseMonTt.getLong(4));

		return row;
	}

	//ResultSet getJnspReseMonTb 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseMonTb() throws Exception{
		getJnspReseMonTb_AXIS = GetB(getJnspReseMonTb.getString(1));
		getJnspReseMonTb_ORDER_STATE = GetB(getJnspReseMonTb.getString(2));
		getJnspReseMonTb_ORDER_DT = GetB(getJnspReseMonTb.getString(3));
		getJnspReseMonTb_CNT = getJnspReseMonTb.getLong(4);
	}

	//ResultSet getJnspReseMonTb 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseMonTb(){
		getJnspReseMonTb_AXIS = "";
		getJnspReseMonTb_ORDER_STATE = "";
		getJnspReseMonTb_ORDER_DT = "";
		getJnspReseMonTb_CNT = 0L;
	}

	//ResultSet getJnspReseMonTb 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseMonTb() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseMonTb.getObject(1));
		row.add(getJnspReseMonTb.getObject(2));
		row.add(getJnspReseMonTb.getObject(3));
		row.add(getJnspReseMonTb.getLong(4));

		return row;
	}

	//ResultSet getJnspReseDay 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseDay() throws Exception{
		getJnspReseDay_AXIS = GetB(getJnspReseDay.getString(1));
		getJnspReseDay_ORDER_STATE = GetB(getJnspReseDay.getString(2));
		getJnspReseDay_ORDER_DT = GetB(getJnspReseDay.getString(3));
		getJnspReseDay_CNT = getJnspReseDay.getLong(4);
	}

	//ResultSet getJnspReseDay 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseDay(){
		getJnspReseDay_AXIS = "";
		getJnspReseDay_ORDER_STATE = "";
		getJnspReseDay_ORDER_DT = "";
		getJnspReseDay_CNT = 0L;
	}

	//ResultSet getJnspReseDay 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseDay() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseDay.getObject(1));
		row.add(getJnspReseDay.getObject(2));
		row.add(getJnspReseDay.getObject(3));
		row.add(getJnspReseDay.getLong(4));

		return row;
	}

	//ResultSet getJnspReseDayTt 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseDayTt() throws Exception{
		getJnspReseDayTt_AXIS = GetB(getJnspReseDayTt.getString(1));
		getJnspReseDayTt_ORDER_STATE = GetB(getJnspReseDayTt.getString(2));
		getJnspReseDayTt_ORDER_DT = GetB(getJnspReseDayTt.getString(3));
		getJnspReseDayTt_CNT = getJnspReseDayTt.getLong(4);
	}

	//ResultSet getJnspReseDayTt 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseDayTt(){
		getJnspReseDayTt_AXIS = "";
		getJnspReseDayTt_ORDER_STATE = "";
		getJnspReseDayTt_ORDER_DT = "";
		getJnspReseDayTt_CNT = 0L;
	}

	//ResultSet getJnspReseDayTt 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseDayTt() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseDayTt.getObject(1));
		row.add(getJnspReseDayTt.getObject(2));
		row.add(getJnspReseDayTt.getObject(3));
		row.add(getJnspReseDayTt.getLong(4));

		return row;
	}

	//ResultSet getJnspReseDayTb 객체에서 필드 값 설정 함수
	private void setFieldVariableFromgetJnspReseDayTb() throws Exception{
		getJnspReseDayTb_AXIS = GetB(getJnspReseDayTb.getString(1));
		getJnspReseDayTb_ORDER_STATE = GetB(getJnspReseDayTb.getString(2));
		getJnspReseDayTb_ORDER_DT = GetB(getJnspReseDayTb.getString(3));
		getJnspReseDayTb_CNT = getJnspReseDayTb.getLong(4);
	}

	//ResultSet getJnspReseDayTb 객체의 필드 값 초기화 함수
	private void setFieldInitFromgetJnspReseDayTb(){
		getJnspReseDayTb_AXIS = "";
		getJnspReseDayTb_ORDER_STATE = "";
		getJnspReseDayTb_ORDER_DT = "";
		getJnspReseDayTb_CNT = 0L;
	}

	//ResultSet getJnspReseDayTb 객체에서 RowData 획득함수
	private List<Object> getAIRowDataFromgetJnspReseDayTb() throws Exception{
		List<Object> row = new ArrayList<Object>();

		row.add(getJnspReseDayTb.getObject(1));
		row.add(getJnspReseDayTb.getObject(2));
		row.add(getJnspReseDayTb.getObject(3));
		row.add(getJnspReseDayTb.getLong(4));

		return row;
	}

	//-----고정 데이터 초기화 함수-----
	private void initConstVarientEx(){
	}

	//----AI_CHART_OBJ32 챠트 데이터 함수----
	private void getChartDataAI_CHART_OBJ32( ) throws Exception{
		//DataSet 객체 생성
		//ResultSet getJnspReseDay 객체의 생성
		if(getJnspReseDay==null){
			getJnspReseDay = stat_getJnspReseDay.executeQuery();
		}else{
			getJnspReseDay.beforeFirst();
		}
		if(!getJnspReseDay.isBeforeFirst()){		//생성된 ResultSet에 레코드가 없으면
			return;
		}

		//계열(Series) 필드 설정
		AI_CHART_OBJ32="\t";
		AI_CHART_OBJ32=AI_CHART_OBJ32 + "CNT\r\n";

		while(getJnspReseDay.next()){   //데이터 루프문
			//데이터 fetch
			setFieldVariableFromgetJnspReseDay();   //RecordSet getJnspReseDay에서 필드 값 설정

			//X축 레이블 필드 설정
			AI_CHART_OBJ32=AI_CHART_OBJ32 + getJnspReseDay_AXIS + "\t";

			//데이터 설정
			AI_CHART_OBJ32=AI_CHART_OBJ32 + getJnspReseDay_CNT + "\r\n";
		}   //while end
	}

	//-----공통 함수 선언-----
	//파라미터 설정 함수
	private void setParam() {
		//SetParam_AutoCode
		reportMode=decoder.getParameterB("reportMode");
		if(reportMode.equals("")) {
			reportMode="HTML";
		}
		manager.setParam("pdfFilename:" + pdfFilename + "," + "excelFilename:" + excelFilename + "," + decoder.getParameterB("reportParams"));
	}

	//상수 데이터 초기화 래퍼 함수
	private void initConstVarient() {
		//InitConstVarient_AutoCode
		initConstVarientEx();
	}

	//배경 이미지 출력 래퍼 함수
	private void getBgImageScript(int nIndex) throws Exception{
		//GetBgImageScript_AutoCode
		getBgImageScriptEx(nIndex);
	}

	//페이지 스킵 함수
	//페이지 스킵시 호출되는 함수로서 페이지 번호를 증가시키고 페이지 지시문 출력 그리고 Y좌표 초기화
	//매개변수에 따라 해당 쉬트의 배경 이미지, 머리글 밴드, 바닥글 밴드 함수 호출
	private void pageSkip(int nIndex) throws Exception{
		//PageSkip_AutoCode
		if(nBandRepeatType==1) {
			int tmpX;
			tmpX=nXCurPos+nBandWidth+nBandOffset;
			nHorRepeatCnt=nHorRepeatCnt+1;
			if(tmpX+nBandWidth>nXEndPos || nHorRepeatCnt==nHorRepeatNum) {
				nYBandStart=nYStartPos;
				nYBandEnd=0;
				nHorRepeatCnt=0;
			}else{
				if(nYBandEnd<nYCurPos) {
					nYBandEnd=nYCurPos;
				}
				nYCurPos=nYBandStart;
				nXCurPos=tmpX;
				return;
			}
		}

		nXCurPos=nXStartPos;
		nYCurPos=nYStartPos;
		nPageNum=nPageNum+1;
		pageWrite(nPageNum);

		switch(nIndex){
			case 10:		//SHEET=Sheet=1   FRAME=0에서 페이지 스킵시
			break;
		}
	}

	//페이지 스킵이 필요한지 검사하는 함수
	private boolean isPageSkip(int nTestPos){
		//IsPageSkip_AutoCode
		if(nTestPos>nYEndPos){
			return true;
		}else{
			return false;
		}
	}

	//문자의 폭을 조사하여 데이터의 출력 높이를 자동적으로 조정하는 함수
	private int getPrintHeight(String data, int[] charWidth, int fontSize, int printWidth, int printHeight, int nMaxHeight, int horMargin, int vertMargin, String styleName) throws Exception {
		//GetPrintHeight_AutoCode
		if(data.equals(isEmpty)){
			return nMaxHeight;
		}
		if(!reportMode.equals("AIScript")) {
			printWidth+=horMargin;
			printHeight+=vertMargin;
			int needHeight=(int)TextHandler.wordWrapTextEx(printWidth, data, styleName, manager.getAIStyleInfo());

			if(needHeight>printHeight) {
				needHeight=needHeight-printHeight;
			}else{
				needHeight=0;
			}

			needHeight=(int)MAX((long)needHeight,(long)nMaxHeight);
			return needHeight;
		}
		else {
			int ch;
			int len = 95;
			int []width = new int[1];
			width[0] = 0;
			int asciiWidth = 0;
			int linePitch = (int)(fontSize*1.4);
			int height = linePitch;
			int size = data.length();
			int ascIndex=0;
			printWidth = printWidth*100;

			for (int i = 0; i < size; i++) {
				ch = data.charAt(i);

				if(ch<32 && !(ch==10 || ch==20 || ch==13)){
					continue;
				}

				if((ch>64 && ch<91) || (ch>96 && ch<123)) {
					if(asciiWidth==0){
						ascIndex=i;
					}

					asciiWidth += charWidth[ch-32];
					if (i==size-1){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i+1, linePitch, printWidth, width, asciiWidth);
					}
				} else if (ch==10 || ch==20 || ch==13) {
					if(asciiWidth>0){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i, linePitch, printWidth, width, asciiWidth);
						asciiWidth = 0;
					}

					width[0] = 0;
					height += linePitch;

					if (ch==13) {
						if ((i != size-1) && (10 == (int)data.charAt(i+1))) {
							i++;
						}
					}
				} else {
					if(asciiWidth>0){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i, linePitch, printWidth, width, asciiWidth);
						asciiWidth=0;
					}

					if(ch>=32 && ch<=127){
						width[0] += charWidth[ch-32];
						if (width[0] > printWidth) {
							if (ch==32)
							{
								width[0] = 0;
							}
							else
							{
								width[0] = charWidth[ch-32];
							}
							height += linePitch;
						}
					}else{
						width[0] += charWidth[len];
						if (width[0] > printWidth) {
							width[0] = charWidth[len];
							height += linePitch;
						}
					}
				}
			}

			if (height>printHeight) {
				height=height-printHeight;
			}else{
				height=0;
			}

			height = (int)MAX((long)height,(long)nMaxHeight);
			return height;
		}
	}

	//영문 단어의 폭을 문자별로 조사하여 데이터의 출력 높이를 조정하는 함수
	private int getWordWrapHeight(String data, int[] charWidth, int nStartIndex, int nEndIndex, int linePitch, int printWidth, int []width, int asciiWidth){
		//GetWordWrapHeight_AutoCode
		int ch;
		int height=0;

		if(asciiWidth>printWidth){
			for(int i=nStartIndex; i<nEndIndex; i++){
				ch=data.charAt(i);
				width[0]+=charWidth[ch-32];
				if(width[0]>printWidth){
					width[0]=charWidth[ch-32];
					height+=linePitch;
				}
			}
		}else{
			width[0]+=asciiWidth;
			if(width[0]>printWidth){
				width[0]=asciiWidth;
				height+=linePitch;
			}
		}

		return height;
	}

	//문자의 폭을 조사하여 데이터의 출력 높이를 자동적으로 조정하고 페이지 초과시 초과하는 데이터 절삭하는 함수
	private int getPrintHeightEx(String data, String[] strSplitData, int[] charWidth, int fontSize, int printWidth, int nExtHeight, int nMaxHeight, int horMargin, int vertMargin, String styleName) throws Exception {
		//GetPrintHeightEx_AutoCode
		if(!reportMode.equals("AIScript")) {
			strSplitData[0]="";
			strSplitData[1]="";
			printWidth+=horMargin;
			nExtHeight+=vertMargin;
			int needHeight=(int)TextHandler.wordWrapTextEx(printWidth, nExtHeight, data, strSplitData, styleName, manager.getAIStyleInfo());

			if(needHeight>nExtHeight) {
				needHeight=nExtHeight;
			}

			needHeight = (int)MAX((long)needHeight,(long)nMaxHeight);
			return needHeight;
		}
		else {
			int ch;
			int i;
			int len = 95;
			int []width = new int[1];
			width[0] = 0;
			int asciiWidth = 0;
			int linePitch = (int)(fontSize*1.4);
			int height = linePitch;
			int size = data.length();
			int ascIndex=0;
			boolean bSplit=false;
			strSplitData[0]="";
			strSplitData[1]="";
			printWidth = printWidth*100;

			for (i = 0; i < size; i++) {
				ch = data.charAt(i);

				if(ch<32 && !(ch==10 || ch==20 || ch==13)){
					continue;
				}

				if ((ch>64 && ch<91) || (ch>96 && ch<123)) {
					if(asciiWidth==0){
						ascIndex=i;
					}

					asciiWidth += charWidth[ch-32];
					if (i==size-1){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i+1, linePitch, printWidth, width, asciiWidth);
						if(height>nExtHeight){
							height=nExtHeight;
							bSplit=true;
							i=ascIndex;
							break;
						}
					}
				}else if (ch==10 || ch==20 || ch==13) {
					if(asciiWidth>0){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i, linePitch, printWidth, width, asciiWidth);
						if(height>nExtHeight){
							height=nExtHeight;
							bSplit=true;
							i=ascIndex;
							break;
						}
						asciiWidth = 0;
					}

					width[0] = 0;
					height += linePitch;
					if (ch==13){
						if ((i != size-1) && (10 == (int)data.charAt(i+1))) {
							i++;
						}
					}

					if(height>nExtHeight){
						height=nExtHeight;
						if(i!=size-1){
							bSplit=true;
							i++;
						}
						break;
					}
				}else{
					if(asciiWidth>0){
						height+=getWordWrapHeight(data, charWidth, ascIndex, i, linePitch, printWidth, width, asciiWidth);
						if(height>nExtHeight){
							height=nExtHeight;
							bSplit=true;
							i=ascIndex;
							break;
						}
						asciiWidth=0;
					}

					if(ch>=32 && ch<=127){
						width[0] += charWidth[ch-32];
						if (width[0] > printWidth) {
							if (ch==32)
							{
								width[0] = 0;
							}
							else
							{
								width[0] = charWidth[ch-32];
							}
							height += linePitch;
						}
					}else{
						width[0] += charWidth[len];
						if (width[0] > printWidth) {
							width[0] = charWidth[len];
							height += linePitch;
						}
					}

					if(height>nExtHeight){
						height=nExtHeight;
						bSplit=true;
						break;
					}
				}
			}

			if(bSplit){
				strSplitData[0]=data.substring(0, i);
				strSplitData[1]=data.substring(i);
			}

			height = (int)MAX((long)height,(long)nMaxHeight);
			return height;
		}
	}

	//스크립트 헤더 출력 함수
	private void getScriptHeader(String paper, String size, String value, String orient, String margin) throws Exception{
		//GetScriptHeader_AutoCode
		manager.scriptWrite("AIReport", "scriptVersion|docVersion|style|title", "32|0|Temporary.style|통계", false);
	}

	private void getSheetPaperInfo(String paper, String size, String value, String orient, String margin) throws Exception{
		//GetSheetPaperInfo_AutoCode
		manager.scriptWrite("PaperInfo", "name|size|value|orientation|margin", paper+"|"+size+"|"+value+"|"+orient+"|"+margin, true);
	}

	private void initSheetVariant(int constStartX, int constStartY, int constEndX, int constEndY, boolean bFirstFrame){
		//InitSheetVariant_AutoCode
		if(bFirstFrame){
			nStartPage=nMaxPage+1;
			nYLastPos=0;
		}
		nPageNum=nStartPage;
		nYStartPos=constStartY;
		nYEndPos=constEndY;
		nYCurPos=nYStartPos;
		nXStartPos=constStartX;
		nXEndPos=constEndX;
		CELL_OBJECT=0;
		DRAW_OBJECT=1;
		nXCurPos=nXStartPos;
	}

	private void initChildSheetVariant(int constStartX, int constStartY, int constEndX, int constEndY, boolean bIsMultiFrame, boolean bIsFirstFrame){
		//InitChildSheetVariant_AutoCode
		nXStartPos=constStartX;
		nXEndPos=constEndX;
		nXCurPos=nXStartPos;
		nYStartPos=constStartY;
		nYEndPos=constEndY;

		if(bIsMultiFrame){
			if(bIsFirstFrame){
				nStartPage=nPageNum;
				nYSavePos=nYCurPos;
				nYLastPos=0;
			}else{
				nPageNum=nStartPage;
				nYCurPos=nYSavePos;
			}
		}else{
			nStartPage=nPageNum;
		}
	}

	//Empty Row만으로 구성되어 출력 위치를 조정하기 위한 VoidBand 함수
	//nRangePos - VoidBand의 출력 영역
	//nIndex - PageSkip 함수 호출시 전달할 쉬트 인덱스
	private boolean voidBandNoneData(int nRangePos, int nIndex) throws Exception{
		//VoidBandNoneData_AutoCode
		int nTestPos;
		nTestPos=nYCurPos+nRangePos;
		if(isPageSkip(nTestPos)){
			pageSkip(nIndex);
			return true;
		}else{
			nYCurPos=nYCurPos+nRangePos;
			return false;
		}
	}

	private String Get0(String str){
		//Get0_AutoCode
		if(str==null){
			return "0";
		}else{
			if(str.equals("")){
				return "0";
			}else{
				return str;
			}
		}
	}

	private String GetB(String str){
		//GetB_AutoCode
		if(str==null){
			return "";
		}else{
			return str;
		}
	}

	private double SUM(double accVal, double tmpVal){
		//SUM(double, double)_AutoCode
		return(accVal+tmpVal);
	}

	private double SUM(double accVal, long tmpVal){
		//SUM(double, long)_AutoCode
		return(accVal+(double)tmpVal);
	}

	private long SUM(long accVal, long tmpVal){
		//SUM(long, long)_AutoCode
		return(accVal+tmpVal);
	}

	private double SUM(long accVal, double tmpVal){
		//SUM(long, double)_AutoCode
		return((double)accVal+tmpVal);
	}

	private long COUNT(long accuCNT){
		//COUNT_AutoCode
		return ++accuCNT;
	}

	private long MIN(long maxV, long maxTempV){
		//MIN(long, long)_AutoCode
		if(maxV<=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MIN(long maxV, double maxTempV){
		//MIN(long, double)_AutoCode
		if((double)maxV<=maxTempV){return (double)maxV;}
		else{return maxTempV;}
	}

	private double MIN(double maxV, double maxTempV){
		//MIN(double, double)_AutoCode
		if(maxV<=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MIN(double maxV, long maxTempV){
		//MIN(double, long)_AutoCode
		if(maxV<=maxTempV){return maxV;}
		else{return (double)maxTempV;}
	}

	private long MAX(long maxV, long maxTempV){
		//MAX(long, long)_AutoCode
		if(maxV>=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MAX(long maxV, double maxTempV){
		//MAX(long, double)_AutoCode
		if((double)maxV>=maxTempV){return (double)maxV;}
		else{return maxTempV;}
	}

	private double MAX(double maxV, double maxTempV){
		//MAX(double, double)_AutoCode
		if(maxV>=maxTempV){return maxV;}
		else{return maxTempV;}
	}

	private double MAX(double maxV, long maxTempV){
		//MAX(double, long)_AutoCode
		if(maxV>=maxTempV){return maxV;}
		else{return (double)maxTempV;}
	}

	private double AVG(double sumVal, long cntVal){
		//AVG(double, long)_AutoCode
		return(sumVal/cntVal);
	}

	private double AVG(long sumVal, long cntVal){
		//AVG(long, long)_AutoCode
		return((double)sumVal/(double)cntVal);
	}

	private String aiReplace(String strAll, String strSrc, String strDest) {
		//AIReplace_AutoCode
		int st = 0, ed = 0;
		final int size = strAll.length();
		ed = strAll.indexOf(strSrc);
		if (ed == -1) return strAll;

		StringBuffer buf = new StringBuffer(size);
		final int len =  strSrc.length();
		do {
			buf.append(strAll.substring(st, ed));
			buf.append(strDest);
			ed += len;
			st = ed;
		} while ((ed = strAll.indexOf(strSrc, ed)) != -1);
		if (st < size) {
			buf.append(strAll.substring(st));
		}
		return buf.toString();
	}

	private String toDate() {
		//ToDate_AutoCode
		SimpleDateFormat dateFormat=new SimpleDateFormat("'#SERVERDATE#'yyyy/MM/dd/HH/mm/ss");
		java.util.Date currentDate = new java.util.Date();
		return dateFormat.format(currentDate);
	}

	final String toKor (String en){
		//ToKor_AutoCode
		if(en==null){
			return "";
		}
		try{
			return new String(en.getBytes("ISO-8859-1"), "utf-8");
		}catch(Exception e){return en;}
	}

	private void objectWrite(int type, int left, int top, int width, int height, String data, String name, String style) throws Exception {
		//ObjectWrite_AutoCode
		manager.scriptWrite(type, left, top, width, height, data, name, style);
	}

	private void propertyWrite(int type, String name, String property, String style) throws Exception {
		//PropertyWrite_AutoCode
		manager.scriptWrite(type, name, property, style);
	}

	private void pageWrite(int pageNum) throws Exception {
		//PageWrite_AutoCode
		manager.scriptWrite("Page", "number", Integer.toString(pageNum), true);
	}

	private void objectCellWrite(String name, String data, int nMoveX, int nMoveY) throws Exception {
		objectWrite(CELL_OBJECT, nMoveX+(nXCurPos+mapLeft.get(name)), nMoveY+(nYCurPos+mapTop.get(name)), mapWidth.get(name), mapHeight.get(name), data, name, mapStyle.get(name));
	}

	public String checkParam(String param) {
		param = param.replace("'", "");
		param = param.replace(";", "");
		param = param.replace("|", "");
		param = param.replace(":", "");
		param = param.replace("+", "");
		param = param.replace("\"", "");
		param = param.replace("\\", "");
		param = param.replace("--", "");
		param = param.replace("/", "");
		param = param.replaceAll("(?i)select", "");
		param = param.replaceAll("(?i)union", "");
		param = param.replaceAll("(?i)delete", "");
		param = param.replaceAll("(?i)update", "");
		param = param.replaceAll("(?i)insert", "");
		param = param.replaceAll("(?i)create", "");
		param = param.replaceAll("(?i)alter", "");
		param = param.replaceAll("(?i)drop", "");
		param = param.replaceAll("(?i)grant", "");
		param = param.replaceAll("(?i)revoke", "");
		param = param.replaceAll("(?i)merge", "");
		return param;
	}

	public String checkAITag(String strText) {
		strText = strText.replace("AI{P}", "^");
		strText = strText.replace("AI{F}", "@");
		strText = strText.replace("AI{S}", "#");
		strText = strText.replace("AI{E}", "=");
		return strText;
	}

	//셀 Rect, Style값 Map 저장
	public void setCellInfoMap_Internal(String name, int left, int top, int width, int height, String style) throws Exception {
		if( left != -1 ) {
			mapLeft.put(name, left);
		}
		if( top != -1 ) {
			mapTop.put(name, top);
		}
		if( width != -1 ) {
			mapWidth.put(name, width);
		}
		if( height != -1 ) {
			mapHeight.put(name, height);
		}
		if( style != null && !style.equals("") ) {
			mapStyle.put(name, style);
		}
	}

	//셀 Rect, Style값 저장
	public void setCellInfoMap() throws Exception {
		mapLeft = new HashMap<String, Integer>();
		mapTop = new HashMap<String, Integer>();
		mapWidth = new HashMap<String, Integer>();
		mapHeight = new HashMap<String, Integer>();
		mapStyle = new HashMap<String, String>();
		setCellInfoMap_Internal("sheet10_A1", 0, 0, 24, 20, "cell_style_0");
		setCellInfoMap_Internal("sheet10_A2", 0, 0, 256, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_J2", 256, 0, 29, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_K2", 285, 0, 29, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_L2", 314, 0, 29, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_M2", 343, 0, 29, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_N2", 372, 0, 29, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_O2", 401, 0, 29, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_P2", 430, 0, 29, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_Q2", 459, 0, 28, 27, "cell_style_2");
		setCellInfoMap_Internal("sheet10_R2", 487, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_S2", 515, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_T2", 543, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_U2", 571, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_V2", 599, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_W2", 627, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_X2", 655, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_Y2", 683, 0, 28, 27, "cell_style_1");
		setCellInfoMap_Internal("sheet10_B20", 24, 372, 348, 20, "cell_style_4");
		setCellInfoMap_Internal("sheet10_N20", 372, 372, 339, 20, "cell_style_5");
		setCellInfoMap_Internal("sheet10_B21", 24, 0, 348, 20, "cell_style_6");
		setCellInfoMap_Internal("sheet10_N21", 372, 0, 339, 20, "cell_style_7");
		setCellInfoMap_Internal("sheet10_B22", 24, 0, 29, 20, "cell_style_8");
	}

	private ResultSet acquireResultSet(CallableStatement cstmt, int cursorIdx) throws SQLException {
		if (cstmt.execute()) {
			if (cursorIdx == -1) {
				return (cstmt.getResultSet());
			} else {
				return ((ResultSet) cstmt.getObject(cursorIdx));
			}
		} else {
			int count;
			if ((count = cstmt.getUpdateCount()) == -1) {
				if (cursorIdx == -1) {
					throw (new UnsupportedOperationException("None result"));
				} else {
					return ((ResultSet) cstmt.getObject(cursorIdx));
				}
			}
			while (true) {
				if (cstmt.getMoreResults()) {
					if (cursorIdx == -1) {
						return (cstmt.getResultSet());
					} else {
						return ((ResultSet) cstmt.getObject(cursorIdx));
					}
				} else if ((count = cstmt.getUpdateCount()) == -1) {
					if (cursorIdx == -1) {
						throw (new UnsupportedOperationException("None result-2"));
					} else {
						return ((ResultSet) cstmt.getObject(cursorIdx));
					}
				}
			}
		}
	}
}
%>
