$.getJSON("/file/banner/bannerData.txt", function(json) {	var bannerHtml = "";	$.each(json, function(key) {		bannerHtml += "<li>";		bannerHtml += "	<a href=\"" + json[key].LINK_URL + "\" target='" + json[key].LINK_TARGET + "' >";		bannerHtml += "		<img src=\"" + json[key].IMG_SRC + "\" width=\"221px\">";		bannerHtml += "	</a>";		bannerHtml += "</li>";	});//	console.info(bannerHtml);	$(".album").append(bannerHtml);	var $list = $('ul.album');	var size = $list.children().outerWidth();	var len = $list.children().length;	var speed = 2000;	var timer = null;	var auto = true;	var cnt = 1;	$list.css('width', len * size);	if (auto) timer = setInterval(autoSlide, speed);	$list.children().bind({		'mouseenter' : function() {			if (!auto)				return false;			clearInterval(timer);			auto = false;		},		'mouseleave' : function() {			timer = setInterval(autoSlide, speed);			auto = true;		}	});	function autoSlide() {		if (cnt > len - 1) {			cnt = 0;		}		$list.animate({			'left' : -(cnt * size) + 'px'		}, 'normal');		cnt++;	};});